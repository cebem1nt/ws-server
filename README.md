# WebSocket Server

This project is a recreation of a basic websocket server in C/C++ without any libraries. Also it has a client, which is a simple webrtc messenger that uses our server to transfer messages between peers while trying to connect (signalling server).

The C++ in this project is used for the data structures like hash set and hash map. Also I wanted to see how to use C++ with C 